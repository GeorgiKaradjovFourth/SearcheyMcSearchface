@using SearcheyMcSearchface.Models
@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h2>Searchy McSearchface</h2>

    <div class="row">
        <div class="col-md-6">
            <h4 class="small">
                Searchy McSearchface searches text, relations and tags (keywords) in all Fourth (Adaco) wikis and documents
            </h4>
        </div>
    </div>
</div>

@Html.Partial("_Search", new SearchViewModel())